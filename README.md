# Calabi ML Server (NER)

FastAPI based Calabi NER server

## Run Locally
1. Install dependencies

```bash
pip install -r requirements.txt
```

2. Run the server

```bash
uvicorn src.main:app --host 0.0.0.0 --reload
```